dev:
  backend: memcache
  namespace: TOTSY_REST_API
  servers:
    - host: localhost
      port: 11211
stg:
  backend: memcache
  namespace: TOTSY_REST_API
  servers:
    - host: localhost
      port: 11211
prd:
  backend: memcache
  namespace: TOTSY_REST_API
  servers:
    - host: localhost
      port: 11211
