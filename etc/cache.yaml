dev:
  backend: memcache
  servers:
    - host: localhost
      port: 11211
stg:
  backend: memcache
  servers:
    - host: localhost
      port: 11211
prd:
  backend: memcache
  servers:
    - host: localhost
      port: 11211
